extends ../layouts/default

block content
  div.jumbotron
    h1 Metrics
    - if (metrics.length)
      - each metric in metrics
        .metric
          h3
            a(href='/metrics/'+metric._id, title=metric.title) #{metric.title}
          p= metric.metricType

    - else
      p.muted No metrics.&nbsp;